# No secrets are required for the MVP.

# Optional app config
APP_ENV=development
AUDIT_LOG_PATH=data/audit-log.jsonl
POLICY_CONFIG_PATH=data/policies.default.json

# No wallet, RPC credential, private key, or Circle credential is used by this MVP.
# Arc Testnet simulation configuration is fixed in source and performs no network call.
