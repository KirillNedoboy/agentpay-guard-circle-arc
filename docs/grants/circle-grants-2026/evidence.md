# Circle Grants 2026 — Evidence Package

This file is the reviewer-readable proof index for the Circle Grants pilot track on
branch `grant/circle-grants-pilot-2026`. It collects the internal engineering
evidence produced by Phases 1–8 of that branch: deterministic policy decisions,
fingerprint-based attribution, replay and policy-drift evidence, a bounded
ExecutionAuthorization envelope, local pilot observability, judge-first UI
presentation, and a documented engineering threat model. This is **internal
engineering evidence**, distinct from external validation (audits, pilot usage,
grant acceptance, partnerships) — see [What this evidence does NOT prove](#what-this-evidence-does-not-prove).
Every claim below is VERIFIED / PROPOSED / NOT YET VALIDATED / NOT IMPLEMENTED;
nothing else.

## Evidence claims

Statuses: **VERIFIED** (observed in code, tests, or assets), **PROPOSED** (target,
not traction), **NOT YET VALIDATED** (external, unverified), **NOT IMPLEMENTED**
(does not exist in this repository).

| Claim | Status | Evidence | Caveat |
| --- | --- | --- | --- |
| Deterministic `ALLOW` / `REVIEW` / `BLOCK` with `BLOCK` > `REVIEW` > `ALLOW` precedence, stable matched rules and reason codes | VERIFIED | [`src/domain/policy/engine.ts`](../../../src/domain/policy/engine.ts), [decision rules](../../decision-rules.md) | Deterministic over the validated intent and loaded policy; it is not a fraud-prevention guarantee. |
| `policyVersion` and deterministic `policyFingerprint` (sha256 over key-sorted stable JSON, computed at evaluation time, never stored in the policy file) | VERIFIED | [`src/domain/policy/policy-fingerprint.ts`](../../../src/domain/policy/policy-fingerprint.ts), [`data/policies.default.json`](../../../data/policies.default.json) (`policyVersion: "3"`) | Fingerprint proves **content identity only**, not authorship or authenticity. |
| `intentFingerprint` (sha256 over the validated intent; key order irrelevant, array order significant) | VERIFIED | [`src/domain/payment-intent/intent-fingerprint.ts`](../../../src/domain/payment-intent/intent-fingerprint.ts) | Binds the literal validated fields; recipient binding is string-level, not identity-level. |
| `ExecutionAuthorization` — deterministic, typed, ALLOW-only envelope built from the persisted audit record + current policy | VERIFIED | [`src/domain/authorization/execution-authorization.ts`](../../../src/domain/authorization/execution-authorization.ts) | Bounded evidence for a **future** adapter; not an executable capability or bearer token. |
| Single-intent amount binding: `maxAmountUSDC` = persisted proposed amount, never the policy cap (`10.00`) | VERIFIED | `execution-authorization.ts` (`audit.amountUSDC ?? audit.amount`); asserted by tests/security-boundaries.test.ts (`T04 binding`); Phase 2 commit `feat: add execution authorization envelope` | No external executor exists to enforce the envelope. |
| Recipient binding: recipient taken from the persisted audit record | VERIFIED | `execution-authorization.ts`; tests/security-boundaries.test.ts (`T03 binding`) | String-level equality only, not proof of real-world recipient identity. |
| Prepare/simulate-only scope: `executionScope` is the literal `["prepare", "simulate"]` | VERIFIED | `execution-authorization.ts`; tests/security-boundaries.test.ts (`T08 scope`); Phase 2 commit | Type-literal enforced; no execute/submit/broadcast/sign/settle values exist. |
| `executionStatus: "not_executed"` | VERIFIED | `execution-authorization.ts`, audit record type, AgentPay Receipt; Phase 1 commit `feat: add versioned policy evidence` | Literal type, not a runtime value. |
| `fundsMoved: false` on every receipt and authorization | VERIFIED | `execution-authorization.ts`, [`src/domain/payment-intent/receipt.ts`](../../../src/domain/payment-intent/receipt.ts) | No funds move anywhere in this repository. |
| Exact replay: same key → `replayed: true`, `replayMismatch: false`, `policyChanged: false`, same `auditId`, same deterministic `authorizationId`, exactly one canonical record | VERIFIED | [`src/domain/audit/audit-log.ts`](../../../src/domain/audit/audit-log.ts), [`src/domain/audit/replay-evidence.ts`](../../../src/domain/audit/replay-evidence.ts), tests/canonical-grant-scenarios.test.ts (REPLAY), tests/security-boundaries.test.ts (T14); Phase 3 commit `feat: add replay and policy drift evidence` | Demonstrates **idempotent replay consistency** with the persisted decision/evidence. It is NOT "independent decision reproducibility" — that is a PROPOSED pilot protocol only. |
| Replay mismatch detection: same key, changed intent → `replayMismatch: true` → stored decision preserved, **no authorization** | VERIFIED | `replay-evidence.ts`, tests/security-boundaries.test.ts (T02/T04/T05/T12) | A changed stored fingerprint field fails the gate; edited non-fingerprint stored fields are trusted as-is (T13 residual). |
| Policy drift detection: changed `policyVersion`/`policyFingerprint` → `policyChanged: true` → **no authorization**; missing stored attribution → `null` → no authorization | VERIFIED | `replay-evidence.ts`, `execution-authorization.ts` (fail-closed), tests/security-boundaries.test.ts (T06/T07); Phase 3 commit | Drift is reported, not silently absorbed. |
| Canonical ALLOW / REVIEW / BLOCK / REPLAY scenarios | VERIFIED | [`examples/scenario-allow-api.json`](../../../examples/scenario-allow-api.json), [`examples/scenario-review-machine.json`](../../../examples/scenario-review-machine.json), [`examples/scenario-block-risky.json`](../../../examples/scenario-block-risky.json), [`examples/scenario-replay.json`](../../../examples/scenario-replay.json), [`tests/canonical-grant-scenarios.test.ts`](../../../tests/canonical-grant-scenarios.test.ts), [canonical-scenarios.md](./canonical-scenarios.md) | Deterministic product evidence fixtures, not pilot usage. |
| Pilot observation log (append-only, privacy-minimal, one line per validated evaluation including replays, references decisions only by `auditId`) | VERIFIED | [`src/domain/observability/evaluation-observation-log.ts`](../../../src/domain/observability/evaluation-observation-log.ts); Phase 5 commit `feat: add minimal pilot observability` | Observation failure is secondary-evidence-only (`observationRecorded: false`); never changes a decision. |
| Pilot metrics (`PilotMetricsSummary`; read-only `GET /api/pilot-metrics`) | VERIFIED | [`src/domain/observability/pilot-metrics.ts`](../../../src/domain/observability/pilot-metrics.ts), [`src/app/api/pilot-metrics/route.ts`](../../../src/app/api/pilot-metrics/route.ts), [pilot-observability.md](./pilot-observability.md) | Local/demo observations only; metrics computed from the two local logs, never external telemetry. |
| Judge-first UI evidence (replay evidence, authorization panel, policy attribution, local pilot metrics) | VERIFIED | [`src/app/demo-client.tsx`](../../../src/app/demo-client.tsx), [`src/app/page.tsx`](../../../src/app/page.tsx); Phase 6 commit `feat: surface grant evidence in judge UI` | Presentation of server-persisted records; not independent server attestation. |
| Threat-model verification (21 threat classes: T01–T15 + ADD-1..ADD-6, each with control, status, verification, residual risk) | VERIFIED | [threat-model.md](./threat-model.md); Phase 7 commit `test: verify payment security boundaries` | **Internal engineering threat-model verification**, NOT an independent external security audit; no "production secure" claim. |
| Security regression suite (22 tests covering replay substitution, binding, drift, legacy, scope, limits, tamper, concurrency, junk input) | VERIFIED | [`tests/security-boundaries.test.ts`](../../../tests/security-boundaries.test.ts); Phase 7 commit | Documents residual risks (expiry metadata-only, no tamper-evidence, self-asserted identity) as well as mitigations. |
| Strict normalized x402 Payment Requirement contract (typed, validated from unknown input, strict unknown-field rejection at top level and in `extra`) | VERIFIED | [`src/domain/x402/payment-requirement.ts`](../../../src/domain/x402/payment-requirement.ts), [`src/domain/x402/payment-requirement-evidence.ts`](../../../src/domain/x402/payment-requirement-evidence.ts), [`tests/x402-payment-requirement.test.ts`](../../../tests/x402-payment-requirement.test.ts), [x402-payment-requirement-contract.md](./x402-payment-requirement-contract.md) | Syntax validation only; no network/contract allowlisting (that is I2). Injected fields (`privateKey`, `signature`, `transactionHash`, `to`, `data`, `execute`, `rpcUrl`) are rejected, never silently dropped. |
| Deterministic requirement digest (`sha256:<64 hex>` over stable-JSON of the validated requirement via `src/lib/stable-json.ts`; key-order independent, no wall clock/randomness/environment) | VERIFIED | `src/domain/x402/payment-requirement.ts`; `src/lib/stable-json.ts`; `tests/x402-payment-requirement.test.ts` | Commits the validated normalized requirement only — never raw input. |
| Deterministic `PaymentRequirementEvidence` (`evidenceType "x402_payment_requirement"`, `version "v1"`, `protocol "x402"`, `x402Version 2`, `requirementDigest`; no signature/keys/tx hash, no timestamp) | VERIFIED | `src/domain/x402/payment-requirement-evidence.ts`; `tests/x402-payment-requirement.test.ts` | Deterministic by construction (no timestamp, no execution state). |
| Association to Guard audit/authorization evidence (`AuthorizedPaymentRequirementEvidence`: `auditId` + `authorizationId` + `requirementDigest`, `executionStatus "not_executed"`, `fundsMoved false`) built by `buildAuthorizedPaymentRequirementEvidence` from an existing `ExecutionAuthorization` + `PaymentRequirementEvidence` | VERIFIED | `src/domain/x402/payment-requirement-evidence.ts`; `tests/x402-payment-requirement.test.ts` | Association/linkage evidence only, NOT an execution approval. |
| I1 performs no execution/network/signing behavior | VERIFIED | `src/domain/x402/*`; static scan (no wallet/keys/RPC/signing code); `tests/x402-payment-requirement.test.ts` | Guard remains keyless; no Gateway call, no settlement, no funds movement. I1 does not implement I2–I6. |
| Runtime execution eligibility gate (local pure gate deciding whether an issued v1 ExecutionAuthorization may become eligible for a future external signer request) | VERIFIED | [`src/domain/x402/execution-security-gate.ts`](../../../src/domain/x402/execution-security-gate.ts), [`tests/x402-execution-security-gate.test.ts`](../../../tests/x402-execution-security-gate.test.ts), [x402-execution-security-gate.md](./x402-execution-security-gate.md) | **Local pure gate only; no signer/network/settlement.** Fail-closed; `now` injected explicitly, never `Date.now()` inside the gate. |
| Direct requirement digest binding (gate recomputes `fingerprintX402PaymentRequirement` and requires equality with `paymentRequirementEvidence.requirementDigest`; stale/mismatched evidence rejected) | VERIFIED | `src/domain/x402/execution-security-gate.ts`; `tests/x402-execution-security-gate.test.ts` | **Local pure gate only; no signer/network/settlement.** Direct (not transitive) commitment; mismatch rejects with a stable reason code. |
| x402-bound ExecutionAuthorization v2 (separate deterministic artifact, `version "v2"`, `parentAuthorizationId` = v1 id, direct `paymentRequirementDigest` commitment; no nonce/signature/transaction/settlement fields) | VERIFIED | [`src/domain/x402/execution-authorization-v2.ts`](../../../src/domain/x402/execution-authorization-v2.ts), `tests/x402-execution-security-gate.test.ts` | **Local pure gate only; no signer/network/settlement.** `executionStatus "not_executed"`, `fundsMoved false`, `eligibility "eligible_for_external_signer_request"`; v1 unchanged. |
| Arc Testnet network/USDC/Gateway-domain allowlist (`policy.x402Execution.allowedRequirements`: `eip155:5042002`, USDC `0x3600000000000000000000000000000000000000`, `GatewayWalletBatched` v1 / `0x0077777d7EBA4688BDeF3E311b846F25870A19B9`, `maxTimeoutSeconds 604900`, `eip3009`, `authorization`) | VERIFIED | `data/policies.default.json` (`x402Execution`), `src/domain/x402/execution-security-gate.ts`, `tests/x402-execution-security-gate.test.ts` | **Local pure gate only; no signer/network/settlement.** Arc Testnet only; I1 validates syntax, I2 enforces the allowlist (e.g. `eip155:84532` is syntactically valid but rejected). |
| Exact decimal-to-atomic amount binding (assetDecimals 6, no floating point, no rounding; >6 meaningful decimals → not representable → reject) | VERIFIED | `src/domain/x402/execution-security-gate.ts`, `src/lib/decimal.ts`, `tests/x402-execution-security-gate.test.ts` | **Local pure gate only; no signer/network/settlement.** Atomic amount must equal the requirement's `amount` exactly. |
| Trusted recipient → payTo gate (`X402RecipientBinding { recipient, payTo }` from trusted adapter/operator configuration, never the untrusted raw 402 request) | VERIFIED | `src/domain/x402/execution-security-gate.ts`, `tests/x402-execution-security-gate.test.ts` | **Local pure gate only; no signer/network/settlement.** No real seller address in the global policy yet. |
| Durable local execution claim (filesystem-backed execution state store; prepared event + bound EIP-3009 nonce written durably; path from `executionStorePath()` — `AGENTPAY_EXECUTION_STORE_PATH` env override, default beside the active audit log; default location gitignored) | VERIFIED | [`src/domain/x402/execution-store.ts`](../../../src/domain/x402/execution-store.ts), `src/lib/paths.ts` (`executionStorePath()`), [`tests/x402-execution-store.test.ts`](../../../tests/x402-execution-store.test.ts), [x402-execution-store.md](./x402-execution-store.md) | **No signer / no Gateway / no settlement.** Durable single-process-and-shared-filesystem persistence only; not distributed consensus, not WORM, not cryptographically tamper-proof. |
| Single-use v2 authorization consumption (first prepared event permanently consumes the v2 authorization; duplicate prepare → `X402_EXECUTION_ALREADY_CONSUMED`, no second event/nonce/signer-eligible result; true across restarts; retry after terminal failure needs a fresh Guard lineage) | VERIFIED | `src/domain/x402/execution-store.ts`, `tests/x402-execution-store.test.ts` | **No signer / no Gateway / no settlement.** Evaluation replay consistency does NOT mean execution may happen twice. |
| Deterministic EIP-3009 nonce binding + registry (`0x` + SHA-256(stable JSON of `{ purpose: "agentpay_eip3009_nonce_v1", authorizationId, paymentRequirementDigest }`), exactly 32 bytes; one-to-one authorizationId ↔ nonce; exclusive registry claims — same-auth claim = recovery, different-auth = `X402_EXECUTION_NONCE_CONFLICT`) | VERIFIED | `src/domain/x402/execution-store.ts`, `tests/x402-execution-store.test.ts` | **No signer / no Gateway / no settlement.** Gateway nonce-enforcement proof is pending I4/I6. |
| Restart-safe shared-filesystem execution state (state reconstructed from persisted immutable events; no module-level memory required; `fs.open(path, "wx")` O_CREAT\|O_EXCL exclusive creation, first process wins, loser re-reads and returns a deterministic conflict) | VERIFIED | `src/domain/x402/execution-store.ts`, `tests/x402-execution-store.test.ts` | **No signer / no Gateway / no settlement.** Scope is multiple processes on one filesystem, not multi-host / distributed consensus. The canonical audit log (T14) still uses the in-process lock — I3 fixes only the NEW execution-state store. |
| Exclusive transition conflict handling (state machine `prepared \| submitted \| confirmed \| failed`; forbidden transitions rejected; `submitted`/`confirmed` are storage primitives for I4/I5 only; fail-closed strict parsing, no automatic repair, no further writes after corrupt history) | VERIFIED | `src/domain/x402/execution-store.ts`, `tests/x402-execution-store.test.ts` | **No signer / no Gateway / no settlement.** `submitted`/`confirmed` persist state labels only; real evidence arrives with I4/I5. |
| Exact EIP-3009 signing-request construction (strict non-secret `X402Eip3009SigningRequest` binding authorizationId / parentAuthorizationId / auditId / paymentRequirementDigest / network / chainId / assetAddress / payTo / amountAtomic / payerAddress / nonce + EIP-712 block, built only from prepared-record evidence + trusted payer + explicit `now`; `signingRequestDigest = sha256:<64 hex>` over the complete normalized request) | VERIFIED | `src/domain/x402/eip3009-signing-request.ts`, `tests/x402-external-signer.test.ts`, [x402-external-signer.md](./x402-external-signer.md) | **Offline only / no Gateway / no RPC / no settlement / no funds moved.** Digest changes for any bound field change. |
| Key-isolated external signer contract (key lives only in the external signer process/tool `scripts/x402-external-signer.mjs`, from its own `AGENTPAY_X402_SIGNER_PRIVATE_KEY` env, never in `src/`/`.env.example`/repo/execution store/audit logs; Guard core knows only the `X402ExternalSigner { sign(request) → response }` interface — no key/mnemonic/key-file/wallet-provider knowledge) | VERIFIED | `src/domain/x402/external-signer.ts`, `scripts/x402-external-signer.mjs`, `tests/x402-external-signer.test.ts` | **Offline only / no Gateway / no RPC / no settlement / no funds moved.** Reference signer only; not production key-management or hardware-wallet support. |
| Actual offline EIP-712 signature test (ephemeral test key generated at test runtime, child-process env, never persisted/printed; stdin one JSON request → stdout one JSON response; signer derives account, verifies address == `request.payerAddress`, signs exactly the supplied typed data, no network) | VERIFIED | `scripts/x402-external-signer.mjs`, `tests/x402-external-signer.test.ts` (63), `tests/x402-external-signer-cli.test.ts` (6) | **Offline only / no Gateway / no RPC / no settlement / no funds moved.** Ephemeral keys only; no key material in the repo. |
| Cryptographic payer recovery (Guard-side strict response-shape validation, `signingRequestDigest` equality, signature encoding, viem `recoverTypedDataAddress` against the exact locally constructed typed data; recovered address semantically equals trusted `X402PayerBinding.payerAddress`; wrong signer / tampered fields rejected with stable `X402_SIGNER_*` reason codes) | VERIFIED | `src/domain/x402/sign-prepared-x402-execution.ts` (`signPreparedX402Execution`), `src/domain/x402/external-signer.ts` (`validateX402ExternalSignerResponse`), `tests/x402-external-signer.test.ts` | **Offline only / no Gateway / no RPC / no settlement / no funds moved.** Verification is local; no payment is proved. |
| Signer payload digest (`signerPayloadDigest = sha256:<64 hex>` over the complete canonical signed payload, including the signature; changed signature/field → different digest) | VERIFIED | `src/domain/x402/external-signer.ts` (`signerPayloadDigest`, `buildX402SignedPaymentPayload`), `tests/x402-external-signer.test.ts` | **Offline only / no Gateway / no RPC / no settlement / no funds moved.** Digest committed, never the signature/payload/private key. |
| I3 submitted-state integration (after verification, `markX402ExecutionSubmitted({ nonce: prepared.nonce, signerPayloadDigest, occurredAt })`; "submitted" = signed payload commitment entered the execution lifecycle, NOT Gateway acceptance; Guard expiry rechecked `now < prepared.authorizationExpiresAt` strictly before the signer call) | VERIFIED | `src/domain/x402/execution-store.ts` (`markX402ExecutionSubmitted`), `src/domain/x402/sign-prepared-x402-execution.ts`, `tests/x402-external-signer.test.ts` | **Offline only / no Gateway / no RPC / no settlement / no funds moved.** Actual Gateway submission begins only in I5; Guard expiry and EIP-3009 `validBefore` are separate controls. |
| Confirmed Gateway EIP-3009 typed-data facts (primary type `TransferWithAuthorization`; domain `{ name: "GatewayWalletBatched", version: "1", chainId, verifyingContract }` — **`chainId` IS included**; `validAfter = now − 600 s`, `validBefore = now + max(maxTimeoutSeconds, 604900)` = 7 days + 100 s buffer; EOA-only via ecrecover, no ERC-1271) | VERIFIED | `src/domain/x402/eip3009-signing-request.ts` (`GATEWAY_EIP712_DOMAIN_NAME`, `GATEWAY_EIP712_DOMAIN_VERSION`, `X402_EIP3009_PRIMARY_TYPE`, `GATEWAY_AUTH_VALIDITY_WINDOW_SECONDS`, `GATEWAY_VALID_AFTER_BACKDATING_SECONDS`), `tests/x402-external-signer.test.ts` | **Offline only / no Gateway / no RPC / no settlement / no funds moved.** Re-verified from the official `@circle-fin/x402-batching` SDK v3.3.0 on 2026-08-16; chainId-in-domain is an addition to the pinned feasibility record (S17). |
| CI and full test suite | VERIFIED | [`.github/workflows/ci.yml`](../../../.github/workflows/ci.yml); 25 test files / 563 tests, all passing (incl. I1 `tests/x402-payment-requirement.test.ts`, 90 tests, I2 `tests/x402-execution-security-gate.test.ts`, 85 tests, I3 `tests/x402-execution-store.test.ts`, 47 tests + `tests/paths.test.ts` +2, and I4 `tests/x402-external-signer.test.ts` 63 + `tests/x402-external-signer-cli.test.ts` 6); lint, typecheck, build, `git diff --check` green | CI runs on push to `main` and `grant/circle-grants-pilot-2026`, and on pull requests targeting `main`; suite reflects the grant branch at the I4 implementation (post-I1 baseline 21 files / 360 tests, post-I2 22 files / 445 tests, post-I3 23 files / 494 tests, post-I4 25 files / 563 tests). Final I4 counts verified by the implementation worker (2026-08-16). |
| Pre-I5 threat-model re-review completed (14-precondition matrix re-classified against the I4 surface; decision GO WITH BLOCKERS; I5 design frozen) | VERIFIED | [pre-i5-security-review.md](./pre-i5-security-review.md); 2026-08-17 | Internal engineering security review, NOT an independent external audit; no runtime code changed; live payment remains FORBIDDEN in I5. |
| Current signing surface reviewed (real local EIP-3009 signing exists ONLY offline in `scripts/x402-external-signer.mjs`; Guard `src/` keyless, no network/RPC/settlement) | VERIFIED | static execution-surface scan 2026-08-17; threat-model.md; evidence.md I4 rows | Bounded reference test signer, NOT production key management. |
| Gateway execution requirements frozen (settle request/response contract, transfer UUID ≠ on-chain hash, batch-level `txHash` null until batched, statuses received/batched/confirmed/completed/failed, nonce-keyed lookup) | VERIFIED | https://developers.circle.com/openapi/gateway.yaml (re-verified 2026-08-17); pre-i5-security-review.md | No live Gateway call was made during the review; integration remains NOT IMPLEMENTED. |
| Duplicate / retry / unknown-outcome model documented (duplicate nonce rejected-not-idempotent with `nonce_already_used`, exact retry response unspecified; no timeout/at-least-once contract; timed-out settle = UNKNOWN, reconcile via `GET /v1/x402/transfers?nonce=`; `remote_outcome_unknown` state required before I5) | VERIFIED | https://developers.circle.com/openapi/gateway.yaml; https://developers.circle.com/gateway/nanopayments/references/sdk.md; pre-i5-security-review.md (2026-08-17) | Circle documents no timeout semantics; post-timeout acceptance is inference, not a guarantee — reconciliation is the required design. |

## Phase implementation ledger

Verified commit ledger for the grant track, in order, on branch
`grant/circle-grants-pilot-2026` (stable main base:
`eb28fe7973cdd3d770de155556c23ee214c6ef33` — merge of PR #3
`integration/ignyte-circle-arc-preview`). Phases 0–8 are on the grant branch only,
NOT on `main`.

| Phase | Commit | Subject |
| --- | --- | --- |
| 0 | `46cfbab` | chore: establish Circle Grants development track |
| 1 | `971f144` | feat: add versioned policy evidence |
| 2 | `b3aa97e` | feat: add execution authorization envelope |
| 3 | `f5ad45d` | feat: add replay and policy drift evidence |
| 4 | `da282ff` | test: add canonical grant scenarios |
| 5 | `25630d4` | feat: add minimal pilot observability |
| 6 | `65d9ef8` | feat: surface grant evidence in judge UI |
| 7 | `068c579` | test: verify payment security boundaries |
| 8 | `09b400f` | docs: assemble Circle Grants evidence package |

This ledger is current through the Phase 8 checkpoint `09b400f`.

## Evidence status matrix

| Item | State |
| --- | --- |
| Policy engine (`ALLOW` / `REVIEW` / `BLOCK`) | VERIFIED |
| Spend controls (per-request, daily, velocity; decimal-safe) | VERIFIED |
| Policy attribution (`policyVersion` + `policyFingerprint` per record) | VERIFIED |
| ExecutionAuthorization (bounded, single-intent, prepare/simulate-only) | VERIFIED |
| Exact replay (one canonical record per key, same audit + authorization) | VERIFIED |
| Pilot observability (observation log + metrics endpoint) | VERIFIED locally |
| Threat model (21 threat classes, internal engineering review) | VERIFIED internal engineering review |
| x402 payment requirement contract + evidence (I1) | VERIFIED |
| x402 execution security gate (I2 — local pure gate; no signer/network/settlement) | VERIFIED locally |
| x402 execution store (I3 — durable, restart-safe, filesystem-backed execution state; no signer/network/settlement) | VERIFIED locally |
| x402 external signer (I4 — offline external EOA signer boundary, real local EIP-712 signature, cryptographic payer recovery, signed payload digest, I3 submitted-state integration; no Gateway/network/settlement) | VERIFIED locally |
| Pre-I5 security re-review (threat model, current signing surface, Gateway execution requirements, duplicate/retry/unknown-outcome model; decision GO WITH BLOCKERS; I5/I6 NOT IMPLEMENTED; no live Gateway integration, no settlement, no funds moved) | VERIFIED internal engineering review (2026-08-17) |
| Design partners | PROPOSED |
| 100–500 pilot intents | PROPOSED |
| Production settlement | NOT IMPLEMENTED |
| Current deployed-demo parity (https://138-124-108-146.nip.io vs grant branch) | NOT YET VALIDATED |
| Current video parity (YouTube/MP4 walkthrough vs x402-first path) | NOT YET VALIDATED |
| External grant eligibility/rules (deadline, amount, chain, forms) | NOT YET VALIDATED |

## What this evidence does NOT prove

Repository evidence proves product capabilities only. It does NOT prove:

- design partners exist or are committed;
- any real pilot usage or customer adoption;
- grant acceptance or grant eligibility under the current official program rules;
- a Circle / Arc / x402 partnership or endorsement;
- live production settlement, execution, or funds movement of any kind;
- that the live demo at https://138-124-108-146.nip.io currently matches this
  branch (only a historical HTTP 200 probe exists), or that the YouTube/MP4
  walkthrough matches the current x402-first reviewer path;
- that the audit log is an immutable, tamper-evident ledger (it is append-only
  JSONL by application convention, in-process only).

All of the above remain PROPOSED or NOT YET VALIDATED. External claims must be
verified against official current sources before any submission.
