# CitePay scope

The CitePay code is retained as a local illustrative source-selection flow. It produces proposed AgentPay Guard intents from mock source cards and never executes a payment.

Canonical product positioning, judge path, and release material are AgentPay Guard/x402-first. CitePay must remain subordinate in UI and documentation.
